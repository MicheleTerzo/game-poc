import { CustomDialogDirective } from './custom-dialog.directive';

describe('CustomDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDialogDirective();
    expect(directive).toBeTruthy();
  });
});
